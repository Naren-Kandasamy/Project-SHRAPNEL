image: container-registry.oracle.com/java/openjdk:25-oracle-linux9

stages:
  - build
  - test
  - deploy

cache:
  paths:
    - .m2/repository

build_job:
  stage: build
  script:
    - ./mvnw compile

test_job:
  stage: test
  script:
    - ./mvnw test

deploy_job:
  stage: deploy
  script:
    - echo "Simulating deployment for SHRAPNEL..."